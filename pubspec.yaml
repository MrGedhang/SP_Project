name: myapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0-278.1.beta

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^3.5.2
  sqflite: ^2.3.2
  path_provider: ^2.1.2
  carousel_slider: ^4.2.1
  path: ^1.8.3 # ✅ Tambahkan ini untuk memperbaiki error `package 'path' isn't a dependency`
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - Images/
